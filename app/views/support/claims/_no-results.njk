<p class="govuk-body">

  {%- if keywords -%}

    {%- if hasFilters -%}

      {%- if selectedFilters.categories.length > 1 or selectedFilters.categories[0].items.length > 1 -%}

        There are no claims for ‘{{ keywords }}’ and the selected filters.

      {%- else -%}

        There are no claims for ‘{{ keywords }}’ and the selected filter.

      {%- endif -%}

    {%- else -%}

      There are no claims for ‘{{ keywords }}’.

    {%- endif -%}

  {%- else -%}

    {%- if hasFilters -%}

      {%- if selectedFilters.categories.length > 1 or selectedFilters.categories[0].items.length > 1 -%}

        There are no claims for the selected filters.

      {%- else -%}

        There are no claims for the selected filter.

      {%- endif -%}

    {%- else -%}

      There are no claims

    {%- endif -%}

  {%- endif -%}

</p>
